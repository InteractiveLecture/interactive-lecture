discovery:
  build: discovery-service

id:
  build: id-service
  links: 
    - "idredis: idredis"
    - "sessionredis: sessionredis"
    - "discovery: discovery-service"  

idredis:
  image: redis

lecture:
  build: lecture-service
  links: 
    - "discovery: discovery-service"
    - "lectureneo4j: neo4j"
    - "sessionredis: sessionredis"

lectureneo4j:
  image: tpires/neo4j


authentication:
  build: authentication-service
  links:
    - "sessionredis: redis"
    - "discovery: discovery-service"
    - "authpostgres: postgres"
      
authpostgres:
  image: postgres

javaevaluation:
  build: java-evaluation-service
  links:
    - "discovery: discovery-service"
    - "sessionredis: sessionredis"
    - "javaevaluationredis: java-evaluation-redis"

javaevaluationredis:
  image: redis

document:
  build: document-service
  links:
    - "discovery: discovery-service"
    - "documentmongo: mongo"
    - "sessionredis: sessionredis"
documentmongo:
  image: mongo

media:
  build: media-service
  links:
    - "discovery: discovery-service"
    - "mediamongo: mongo"
    - "sessionredis: sessionredis"

mediamongo:
  image: mongo

acl:
  build: acl-service
  links:
    - "discovery: discovery-service"
    - "aclneo4j: neo4j"
    - "sessionredis: sessionredis"

webuserfrontend:
  build: user-webui-service

webadminfrontend:
  build: admin-webui-service

aclneo4j:
  image: tpires/neo4j

sessionredis:
  image: redis
